#include <stdio.h>

  int main()
{
  int number;

  printf("Enter any number to find the absolute value for : ");
  scanf("%d",&number);
  if(number<0)
{
  number=(-1)*number;
  printf("Absolute value is =%d",number);
}

{if(number>0)

    printf("\nAbsolute value is =%d",number);

}
  return (0);
}
